;;; init-bazel.el --- Bazel editing -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(require-package 'bazel)
(setq bazel-mode-buildifier-before-save t)
(provide 'init-bazel)
;;; init-bazel.el ends here
